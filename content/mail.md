## 一封暂时没有地址的来信

这里将来可以放邮箱、社交账号或留言入口。为了避免误导，原型没有填入虚构邮箱，也没有做一个实际上不能提交的表单。

## 当前不会发生的事情

- 不上传姓名、邮箱或任何留言。
- 不调用 AI 聊天接口。
- 不把浏览记录发送到第三方分析服务。

浏览器只会在本地保存探索进度。3D 场景需要向 CDN 请求 Three.js 库文件；第三方网络请求并不等于完全离线运行。

确定真实联系方式后，可以先增加一个简单的邮件链接，再决定是否需要带反垃圾与隐私说明的留言服务。
